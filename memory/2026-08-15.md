# 2026-08-15

## PropNex Telegram link on all article pages (Aug 15)
- User: "https://t.me/PropNexPropertyNewsUpdate/9541 add this to all article page"
- Post = PropNex news broadcast: **Orchard Road facelift** — Istana Park, Dhoby Ghaut Green, Penang Rd areas to form new park + Orchid Park pedestrian zone.
- Script `/tmp/artadd/add-tg-link.mjs` (note: lives in /tmp, may be wiped — pattern is re-runnable, skip if already contains link):
  - Injects before `</body>`: centered block "📰 Latest market news: [Orchard Road facelift — Istana Park, Dhoby Ghaut Green & Penang Rd to form new park](link)" styled to match dark article theme (#c9a84c accent).
- **193 files injected, 0 failed, 1 already had link** across:
  - HQ jetleechannel.sg/articles/ (20) + 24 project /articles/ dirs (incl. TheHillshore)
  - thomson-reserve-direct-developer.com/articles/ (27 incl. stray (1).html + index19july.html)
  - loyangvalleyresidences-official.com/articles/ (21 incl. indexold.html + changi-airport-t5 + is-loyang-good-investment etc.)
- Verified live via curl (grep counts 1 on samples). FTP creds: main Jetleechannel12345& / Thomson12345& / Loyang12345& all working.

## The Collective at One Sophia — new site built (Aug 15, 2026)
- **URL**: https://jetleechannel.sg/theColletive/ (user-specified camelCase!)
- **Project**: 367 units, D09 Orchard/River Valley, 1A & 1B Sophia Road (former Peace Centre), Singhaiyi (Sophia Residential Pte Ltd), 99yr from 6 Dec 2023, TOP Dec 2029, from $1.25M / $2,808 psf. 4 MRT within 700m (Bencoolen 450m, Rochor 500m, Dhoby Ghaut 600m, Bras Basah 700m).
- **Pages built**: homepage (hero + gallery + 15 floorplans + balance table + VR tours + siteplan + amenities + features + FAQ + Web3Forms contact), `price/` (gold 🔥 price cards + live balance table + VVIP form + WA float), `articles/` (index + 2 articles: buyers guide + Sophia Road hotspot, both with PropNex Telegram link).
- **Floating WA button** ✅ on ALL pages (wa.me/6587649315).
- **Assets**: images/ (12 jpg), images/floorplans/ (26 png), docs/ (e-brochure 21.9MB, factsheet 2.1MB).
- **FTP**: uploaded 45 files via `thecolletive/upload.mjs` to `/theColletive/` (needs `--ftp-create-dirs` for new dirs!). All 200 live.
- **Daily updater**: added `thecolletive` to `scripts/update-balance-units.mjs` SITES (projectId `03c7e19648714d9b88e7002ba6d4c0ec`, ftpDir `theColletive` — **capital C!**, displayMap 10 types, extraFiles: price/index.html). Tested: table + date auto-update works on both pages.
- **HQ integration**: added to HQ homepage D09 group (site-jetleechannel.html), `/projects/` (projects-live.html), `/new-launch/` (new-launch-index.html), sitemap.xml (+5 URLs). All uploaded & verified live.
- Web3Forms key on homepage: `80bb82c8-aa68-4974-b63b-66bac63a8016`; price page: `f47a3688-6848-4acc-a968-5174fd191919`.

## The Collective — location map added (Aug 15, afternoon)
- User sent a location map image (1233×1280 JPEG, original in `/home/ubuntu/.openclaw/media/inbound/698c3572-f101-413a-a6f9-caae145eb438.jpg`) and said "put this in location page. Put this first then continue the current info".
- Optimized → `thecolletive/images/location-map.jpg` (1000px, 356KB), uploaded to `/theColletive/images/location-map.jpg`.
- Homepage Location section (`#siteplan`): added the map **full-width FIRST** (before the existing 2 site-plan images), current L1/L3 site plans continue below it. Uploaded `thecolletive/index.html` + verified live via curl.
- Note: image viewing tool was unavailable this run (invalid base64) — worked from file dimensions via file command.

## Luxus Hills mobile menu fix (Aug 15, evening)
- User reported (screenshot = Luxus Hills luxushill10): "Under info page: can't see the scroll down subpages" — the mobile menu INFO dropdown sub-links were cut off on small phones.
- Root cause: on short phone viewports (e.g. 320×568) the expanded INFO dropdown (5 sub-links, ~267px) pushed menu content (635px) past the visible area (495px). Menu had overflow-y:auto but used `height: calc(100vh - 72px)` which misbehaves on iOS Safari (URL bar) — and dropdown max-height 340px made it cramped.
- Fix in luxushill10/index.html: ① added `height/max-height: calc(100dvh - 72px)` fallback for iOS; ② added `overflow-x: hidden`; ③ dropdown mobile max-height 340px → 500px. Uploaded via FTP (226) + verified live (3 markers found, dropdown expands to 500px, menu scrolls 140px, 5 sub-links + 14 total links).
- Verified working: INFO tap expands dropdown, sub-link tap closes menu + scrolls to section (tested at 320×568, 360×740, 591×1280).

## Case-insensitive redirects for camelCase project dirs (Aug 15, night)
- User found: `jetleechannel.sg/belgraviaAce/` (lowercase b) showed the HQ homepage, but `BelgraviaAce` (capital B) showed the Belgravia site. Cause: Linux server = case-sensitive folder names; only `BelgraviaAce` exists on FTP → lowercase request fell through to SPA catch-all (index.html = HQ).
- Root .htaccess already had a pattern for OneMarinaGardens (`^onemarinagardens(/.*)?$ → /OneMarinaGardens$1 [R=301,L,NC]`). Downloaded live .htaccess, added same pattern for:
  - `belgraviaace` → `/BelgraviaAce`
  - `thecolletive` → `/theColletive`
  - `thesierra` → `/TheSierra`
  - `theorie` → `/TheOrie`
- Also added those 4 to the project-dirs skip rule list. Uploaded via FTP (226).
- Verified live: all 4 lowercase variants now 301 → correct case; subpaths (e.g. `thecolletive/price/`) redirect too. `aurea` lowercase worked already (separate lowercase `aurea` folder exists on FTP).
- Lesson: when creating new project dirs with camelCase names (theColletive, TheSierra, TheOrie, BelgraviaAce, OneMarinaGardens), add a case-insensitive RewriteRule in root .htaccess so lowercase typing doesn't hit the HQ fallback. NOTE: .htaccess copy kept at /tmp/htaccess-live (may be wiped).

## Loyang weekly articles automation (Aug 15-16, night)
- User request: "add 2 articles per week (Mon & Fri) to loyangvalleyresidences-official.com article page, claw from https://t.me/PropNexPropertyNewsUpdate"
- **Cron set**: `0 9 * * 1,5` → `node scripts/loyang-weekly-articles.mjs` (Mon & Fri 9am SGT), logs to `loyang-articles/cron.log`.
- **Generator**: `scripts/loyang-weekly-articles.mjs` — fetches t.me/s/PropNexPropertyNewsUpdate (16 posts), picks freshest unused post (state in `loyang-articles/.last-news.json`), generates a Loyang-tied article (GA G-MLD5XYQ2HK, Article schema, FAQ, CTA wa.me/6587649315, facts panel, source link), uploads via FTP (user loyangvalleyresidences-official.com / Loyang12345&), rebuilds index via `scripts/rebuild-loyang-index.mjs`, sends Telegram notification to Jetlee (bot 8124287935:AAHlC8ylOK8IEuSQRLjKGJKLYaMu77ndHsU, chat 87383567).
- **Key parsing fix**: Telegram posts join sentences without punctuation ("global wealth<br/></b>The Republic..." and "2023Analysts", "boom Economy"). Parser now: `<br/></b>` → `.</b> ` (sentence end), camelCase `([a-z0-9])([A-Z])` → `$1. $2`, fallback splits at digit+space+Capital and lowercase→Capital (≥5 chars + ≥25 tail, skip connectors).
- **Index**: rebuilt dynamically from FTP listing (23 articles live, no duplicates). `scripts/rebuild-loyang-index.mjs` scans FTP /articles/ (regex matches `-rw-r--r-- 1 owner group size Mon DD HH:MM name`), fetches each h1/title/desc/datePublished, builds cards. Rebuild before each upload → auto-dedupes.
- **FTP delete quirk**: `-Q "DELE path"` needs NO `--ftp-ssl` and path WITHOUT leading slash (e.g. `DELE articles/foo.html`) for 226; with --ftp-ssl returns 234 (no-op). Use plain curl for deletes.
- **3 articles published during testing** (all live): Sentosa Cove resale losses, GDP forecast 4.5-5.5% on AI boom, MAS family office tax rules. All with clean titles.
- **Index order note**: FTP listing order isn't strictly mtime-sorted; reverse() puts newest near top (Sentosa/GDP first). Acceptable.
