# 2026-08-10 — Hero Lightening Completion (final day of the pass)

## Hero Lightening (Aug 9-10) — DONE, all 22 projects verified live
All 22 project homepages on jetleechannel.sg lightened (hero no longer dark/gloomy) — EXCEPT luxushill10 (user excluded, already bright). Full per-project pattern map already in MEMORY.md "Hero Lightening (Aug 9, 2026)" entry.

### Bugs found & fixed during this pass (IMPORTANT lessons)
1. **lighten-heroes.mjs write bug**: script only wrote the file when `src.length !== origLen`. Many replacements are EQUAL-LENGTH (`.85`→`.45`, `#0E0D0B`→`#241F18`), so 6 files printed ✅ replaced but were **never persisted to disk**: hudsonplace, hougangcentral, lucernegrand, dunearnhouse, generations-tannery, + arcady. Fixed: write when `src !== fs.readFileSync(file,'utf8')` instead of length comparison. Applied via direct one-off exact-string edits + re-upload.
2. **arcady** was missed for the same reason (equal-length hex swap `#0E0D0B`→`#241F18`); fixed directly.
3. Run-2 "⚠️ NOT FOUND" warnings were expected — run 1 already modified those files (script is non-idempotent split/join). Not failures.

### Upload & live verification
- Created `upload-lightened.mjs` (local→FTP path map for all 22 files, `curl -T`, exit 1 on any failure). All 22 uploads got 226 OK.
- Verified LIVE with cache-busted curl (extra query param) on all 22 URLs: old dark values = 0 occurrences, new bright values present. arcady initially failed verification → root cause was local file not updated (write bug) → fixed + re-uploaded + re-verified ✅.
- Told user to hard-refresh (Ctrl+Shift+R) since browser cache may still show dark heroes.

### File path notes
- Subdir files needing explicit paths in scripts: promenadepeak/index.html, rivergreen/index.html, newportresidences/index.html (path fix applied before run 2).
- Stale/unused local copies that still contain OLD dark values and are NOT uploaded (do not confuse with active files): site-eltasingapore.html, site-thecontinuum.html, velabay/, site-lentor-live.html, site-dunearnhouse-preview.html, site-elta-tofix.html, generations-tannery/index.html (active file is site-generations-tannery.html).

### Still pending (from prior summary)
- Update deployment generators (deploy-p1-articles.mjs, generate-p1-articles.mjs) to include `upperhouse`.
- Optional Luxus items: brighten other dusk sections, Luxus SEO article.

## Arcady images added (Aug 10, ~00:10 SGT)
- User noticed arcady has no images at all → found official render/facility images from the-arcady.com.sg, optimized + uploaded 5 to `/arcady/images/`.
- Hero now uses hero-facade.jpg (facade aerial view) with light overlay; added Gallery section (5 cards) + nav GALLERY link; fixed og:image (was pointing to non-existent arcady-og.jpg).
- Verified live via curl (200s) + browser screenshots (desktop + mobile hero render, gallery grid renders).

## Arcady images v2 — ecoprop official set (Aug 10, ~00:25 SGT)
- User sent ecoprop share link: `https://share.ecoprop.com/The-Arcady-at-Boon-Keng/R007613B` (their own agent share page).
- Scraped via browser JS: 41 images on img.singmap.com CDN. Downloaded drone aerial (7313×3657!), exterior view, penthouse interior, Arcady Club, Kids Club.
- **Hero switched to drone-hero.jpg** (optimized 1600×800, 344KB, official developer aerial) — big upgrade over facade shot.
- Gallery expanded 5 → **9 images**: drone, exterior, penthouse, pool, Arcady Club, Kids Club, Gourmet Vista, Sky Gym, Botanic Club. og:image → drone-hero.jpg.
- All 9 verified 200 + browser-loaded (naturalWidth > 0). Local copies kept in `arcady-eco/`. Old hero-facade.jpg still on server (now only used as a gallery click-target? no — removed from gallery; harmless leftover).
