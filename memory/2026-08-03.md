# 2026-08-03 Daily Notes

## Grand Dunman weekly cron — fixed (4 consecutive timeouts)
- Cron job was failing every Monday 9am with "job execution timed out (last phase: model-call-started)"
- Root cause: script's fetch had 15s AbortSignal timeout; source site granddunman.jetleeproperty.sg is flaky (first request often takes >15s, HTTPS sometimes hangs)
- Fix 1: scripts/update-granddunman.mjs — fetchPage() now retries up to 3x, 30s timeout per attempt, 5s/10s backoff
- Fix 2: cron job payload timeoutSeconds bumped 90 → 300
- Verified: script runs clean, fetched data (80/80 units, updated 3 Aug 2026), no changes needed vs Jul 30 state
- Next run: Mon 9am SGT. Destination jetleechannel.sg/granddunman/ is fine (200)
