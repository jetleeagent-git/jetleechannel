# 2026-06-24

## Dunearn House — Floor Plan Gallery & Preview Deploy

### What happened
- Jetlee sent FTP credentials via PDF; updated TOOLS.md with Dunearn House FTP: user `u851958941.dunearnhouse.jetleechannel.sg`, pass `Dunearnhouse87649315$` (web root = `/public_html`)
- Deployed preview to `https://dunearnhouse.jetleechannel.sg/preview/` — floor plan images (18 PNGs) + site plan + preview HTML
- **Bug**: onclick on floor plan cards didn't work in Telegram inline browser — tap saved to clipboard instead of opening
- **Fix**: replaced `<div onclick>` with `<a href>` links (target="_blank") + kept JS lightbox for desktop. Re-uploaded.
- Second bug: tabs didn't switch because `showFPType()` function was accidentally removed when cleaning up lightbox JS
- **Fix**: Added `showFPType()` back — tabs now switch correctly
- **Final version**: Inline floor plan images at readable size + "View Full Size ↗" gold buttons + tabbed type switcher all working
- **LIVE DEPLOYED** to `https://dunearnhouse.jetleechannel.sg/` ✅
- Current index backed up on server as `index-backup-24june2026.html` and locally as `current-live-backup.html`
- Deploy script `deploy.sh` updated: added Dunearn House entry with `public_html` root
- TOOLS.md updated with Dunearn FTP credentials

### Key notes
- FTP root for Dunearn House is `/public_html` (NOT `/` like other 7 sites)
- Floor plan images at `https://dunearnhouse.jetleechannel.sg/images/dunearnhouse/floorplans/`
- Deploy script now supports `bash deploy.sh dunearnhouse`
