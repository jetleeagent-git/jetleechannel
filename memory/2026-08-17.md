## GSC "pages not indexed" fix (Aug 17)
Jetlee showed Google Search Console screenshots: 4 pages not indexed, 4 reasons (404, Duplicate without canonical, Google chose different canonical, Crawled-not-indexed).
**Root causes found & fixed:**
1. **Aurea case-mismatch 404**: sitemap had `aurea/` (lowercase) but live site canonical is `Aurea/` (capital). Lowercase path 301'd, capital `/Aurea/articles/` was EMPTY → 404. Fixed: uploaded 4 aurea articles (with corrected `Aurea/` canonicals + backlinks) to `/Aurea/articles/`, fixed sitemap aurea entries to capital.
2. **Missing canonicals (23 pages)**: HQ articles (20) + dunearnhouse/ + narraresidences/ + thesen/ had no canonical tags. Injected `<link rel="canonical">` via script, re-uploaded via FTP. Also 1 Loyang article (article-hdb-waitout-removed.html) fixed.
3. Sitemap re-uploaded; all 90 jetleechannel URLs + 28 thomson + 24 loyang now return 200 and have canonicals.
4. IndexNow pinged (200 OK) for all fixed URLs on all 3 domains.
**Lesson**: always match sitemap URL case exactly to live canonical; verify new article dirs get uploaded (aurea articles were generated Aug 16 but never uploaded).
## ⚠️ TheSierra → TheSerra URL rename (Aug 17)
Jetlee caught a spelling mistake: project is "The Serra Residences" but URL was `/TheSierra/`. Renamed to `/TheSerra/`.
**Done:**
- Created `/TheSerra/` on server (index + articles + images), uploaded all files (updated canonicals → TheSerra)
- .htaccess: `^thesierra` and `^TheSierra` → 301 to `/TheSerra`; exception list updated
- Sitemap: TheSierra → TheSerra + added 4 article URLs (95 total)
- HQ homepage + /projects/ + /new-launch/ internal links → TheSerra
- deploy.sh SITE_SERRA ftp_root → TheSerra; scripts/regenerate-sitemap.mjs + inject-thomson-banner.mjs updated
- Old `/TheSierra/*` URLs 301 → `/TheSerra/*` (images too, cache-buster confirmed)
- IndexNow pinged (200 OK)
- All 95 sitemap URLs verified 200
**NOTE**: Local working folder stays `thesierra/` (label only, deploy maps to TheSerra FTP path). Old `/TheSierra/` dir still on server (files served 200 for cached image hits but now 301 via htaccess).
