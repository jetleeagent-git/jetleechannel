# 2026-07-21 — Tuesday

## Grand Dunman — Big Mistake

A sub-agent built granddunman.jetleechannel.sg (later moved to jetleechannel.sg/granddunman/). It only uploaded index.html — no images at all. When Jetlee reported broken images, I:
1. **First attempt**: Downloaded 20+ random Grand Dunman artist impressions from public developer galleries and uploaded them. **Bad call.**
2. Jetlee pointed out: "all these are not floor plans" and "don't put for the sake of putting image"
3. **Second attempt**: Found real floor plans from grandsdunman.com.sg, uploaded them, but Cloudflare cached old versions. Renamed files to bypass cache.
4. Jetlee replied: "home page also no background image sigh 😞"
5. **Final fix**: Removed ALL images from the site entirely. No broken icons, just placeholder text. Clean slate until Jetlee provides official assets.
6. **Lesson**: Don't scrape random images to fill a page. Better to have clean placeholder text than wrong images.

## Chinese — Stopped

Jetlee told me to stop typing Chinese (after I kept typing it). He doesn't read Chinese. I stopped and apologized. Won't happen again.

## Lucerne Grand — JLD Images Fixed

The lucernegrand page had 2 external JLD images from `jld.gov.sg` returning 403. Jetlee sent 3 replacement images. Uploaded them and updated HTML:
- `lg-jld-panorama.jpg` (hero banner area)
- `lg-jld-overview.jpg` (JLD overview map)
- `lg-jld-location-map.jpg` (also set as OG/twitter/schema image)
- Also fixed the OG:image URL which was still pointing to the old subdomain

## Amberwood — Mobile Navigation

Jetlee reported no navigation button on mobile. The CSS had `.nav-links { display: none }` on mobile with no toggle. Fixed by:
- Adding hamburger button (☰ → ✕ toggle) visible only on mobile
- Fullscreen overlay menu with centered links
- Tap-to-close on link click
- Deployed to FTP

## Communication Notes
- Jetlee is direct and decisive. When he's frustrated, just fix it and move on.
- Don't overthink. Don't explain. Just fix.
- No Chinese.
- If a sub-agent builds something, always verify ALL assets are uploaded (images, CSS, JS), not just index.html.
