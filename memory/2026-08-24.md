# 2026-08-24/25 — 3-Site Article Pipeline (loyang + thomson + HQ) + GSC status

## User instruction (Aug 24, 23:34 SGT)
"stop updating articles in continuum, narra residences, aurea website. just continue update in loyangvalleyresidences-official.com and thomson-reserve-direct-developer.com and jetleechannel.sg article pages. 3 website ONLY"

## GSC performance question
Jetlee asked about thomson + loyang performance in Google Search Console.
**BLOCKED**: `.gsc/` OAuth token is dead (invalid_grant / 401). Fresh PKCE consent URL prepared in `.gsc/pkce.json` (state `8RHfCv7f2icBcinDhbkvAQ`, verifier `wTQSJHShuiUaVouL24pjvO64M-pluxSNUHF60gP4-bk`). Redirect `http://localhost`. Needs Jetlee to click consent link and paste the code. Client ID in `.gsc/credentials.json`.

## What was done
### 1. New generator scripts built (based on proven loyang pattern)
- `scripts/thomson-weekly-articles.mjs` — thomson-reserve-direct-developer.com, GA G-S46Y1ZCYJH, FTP u851958941.thomson-reserve-direct-developer.com:Thomson12345&, cream/Georgia serif style, blinking Articles nav. Inserts card at top of "🔥 Latest Articles" (does NOT rebuild index — preserves curated categories).
- `scripts/hq-weekly-articles.mjs` — jetleechannel.sg, GA G-6LFG5HCYKP, FTP u851958941.jetleechannel.sg:Jetleechannel12345&, dark-green Playfair style. Every article ends with Thomson Reserve promo box (user rule). Inserts card at top of `.article-list` (does NOT rebuild — preserves header featured cards).
- `scripts/loyang-weekly-articles.mjs` — updated: now checks shared pool (`.shared-used.json`) to avoid duplicating posts already used by thomson/HQ.
- `scripts/weekly-articles.mjs` — **stripped thecontinuum/narraresidences/aurea** (only loyang remains).

### 2. Shared pool prevents cross-site duplicates
`loyang-articles/.shared-used.json` — all 3 generators read/write it. Posts consumed by any site are skipped by the others. Capped at 60. Seeded 9557 (garbled "Vers for old HDB flats" headline) so no site publishes broken titles.

### 3. Title-split improvement
Added SENTENCE_STARTERS set + run-on-headline splitter (split at capitalized common word after lowercase, e.g. "...homes Portion of 13ha" → clean title). Applied to all 3 generators.

### 4. Cron jobs created (OpenClaw cron, only reliable scheduler in container)
- `articles-loyang-mwf` — Mon & Fri 9:05 SGT
- `articles-thomson-mwf` — Mon & Fri 9:10 SGT
- `articles-hq-mwf` — Mon & Fri 9:15 SGT
All run the node script directly; scripts self-notify Telegram. NOTE: system crontab wiped Aug 20; container has NO cron daemon — OpenClaw cron is the only option.

### 5. First live publish batch (all verified 200)
- thomson: post 9558 → "Freehold Sembawang apartments up for collective sale at S$130m"
- HQ: post 9559 → "Kallang Distripark to be redeveloped for thousands of new private homes"
- loyang (manual run): post 9560 → "Father-and-son duo Raj Kumar and Kishin in exclusive due diligence to buy Scotts Square"
- loyang (cron trigger test): post 9561 → "Luxury eldercare from $8k a month at S'pore's first private assisted living project"

## Deepseek billing scare (RESOLVED)
Both old cron jobs (balance-units-mwf, grand-dunman-mwf) failed 8/24 with "deepseek out of credits / provider in cooldown (billing)". Tested API directly 8/25 05:52 SGT — **deepseek works now** (cooldown cleared). OpenAI key (sk-proj...) is the one that's actually out of credits (insufficient_quota). Leave model config as deepseek.

## FTP passwords verified working this session
- thomson-reserve-direct-developer.com: `u851958941.thomson-reserve-direct-developer.com` / `Thomson12345&`
- jetleechannel.sg: `u851958941.jetleechannel.sg` / `Jetleechannel12345&`
- loyangvalleyresidences-official.com: `u851958941.loyangvalleyresidences-official.com` / `Loyang12345&`

## Open items
- GSC numbers still unavailable until Jetlee re-consents OAuth.
- Continuum/narra/aurea article pages remain live but no longer get new articles (per instruction).
