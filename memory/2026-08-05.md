# 2026-08-05

## SEO/AEO/Indexing Push — 3 Sites (jetleechannel.sg, thomson-reserve-direct-developer.com, loyangvalleyresidences-official.com)

### Completed (all verified live)
- **Sitemaps regenerated**: jetleechannel.sg 41→**58 URLs** (25 projects + 29 articles + 4 core; removed dead subdomains, fixed /elta/ not /eltasingapore/, added 16 missing project pages + 8 articles) via `scripts/regenerate-sitemap.mjs`. Thomson 23→**25**, Loyang 8→**22** (was hiding 12 articles!) via `scripts/regenerate-standalone-sitemaps.mjs`.
- **Dead subdomain refs fixed on jetleechannel.sg homepage**: schema JSON-LD `url` fields + project card links now canonical paths (eltasingapore→/elta/, lentorgardens→/lentor-gardens/, lucernegrand→/lucernegrand/, hudsonplace→/hudsonplace/, hougangcentral→/hougangcentral/, amberwood→/amberwood/, thomsonreserve→thomson-reserve-direct-developer.com). Edited live homepage at `/tmp/jet-homepage-live.html` (local index.html ≠ live version).
- **dunearnhouse.jetleechannel.sg was serving duplicate content (200)** → added `.htaccess` 301 redirect → `https://jetleechannel.sg/dunearnhouse/` (uploaded to that subdomain's own FTP account `u851958941.dunearnhouse.jetleechannel.sg`). Old subdomains thomsonreserve/lentorgardens/lucernegrand/hudsonplace/hougangcentral/amberwood already 301 correctly. eltasingapore.jetleechannel.sg = 525 dead (Hostinger SSL issue, can't fix via FTP; dropped from sitemap + homepage, Google will drop it naturally).
- **IndexNow setup on all 3 domains**: key file `f2f51fad679de9eda3426492625c4260` live on all 3 + **89 URLs submitted** (202 accepted). NOTE: IndexNow key is now in MEMORY.md.
- **Loyang homepage had ZERO article links** (19 articles orphaned!) → added "Insights & Guides" section with 6 article links before footer. Also confirmed Thomson links 9+ articles from homepage (fine).
- **Article schema added to ALL 40 real articles** (Thomson 22 + Loyang 18) via `scripts/add-article-schema.mjs`: `Article` + `FAQPage` JSON-LD extracted from existing `<p class="faq-q">` markup. ⚠️ FIRST RUN HAD A BUG: `upload()` used `file.split('/').pop()` which uploaded to junk filenames `schema-<site>-<ts>-<rand>.html` instead of the real article names — 34 junk files created, ~0 real uploads. FIXED: pass `article` name explicitly to `upload()`, deleted 34 junk files via python ftplib (curl -Q DELE doesn't work), re-ran → all 40 articles verified with schema on FTP + live. Also rebuilt Thomson price-guide-2026.html (was 0 bytes, done separately). Homepages have own schema (Thomson 4 blocks, Loyang 2). JSON validated well-formed.
- **Rebuilt Thomson `articles/price-guide-2026.html`** — was **0 bytes (broken empty page)** linked from homepage + sitemap. Rebuilt fully (15.8KB) with pricing tables, FAQ, schema, matching site template. Junk `(1).html` was 404 already, not in sitemap — no action.
- **Loyang homepage 1.78MB → 95KB (−95%)**: found **2 massive base64 images embedded in HTML** (1MB PNG + 190KB JPG) → extracted, converted to WebP (60KB + 75KB), replaced with `<picture>` refs. Plus 4 hero images → WebP (~35% smaller each). All 6 picture blocks verified valid, zero base64 remaining.
- **Thomson images**: 3 heavy imgs → WebP with `<picture>` + JPG fallback (img-03 location map 1400x1116 → 228KB WebP q70; og:image + schema image kept as JPG for compat).
- **Browser screenshots verified** both Loyang + Thomson render perfectly after changes.
- jetleechannel.sg homepage already had strong AEO schema (FAQPage 6 Q&As, 14 RealEstateListings, Breadcrumb, Organization) — no changes needed there.

### Key gotchas
- Loyang HTML is huge with inline styles; base64 images were the weight culprit, NOT CSS/JS.
- Curl `%{size_download}` on HEAD returns 0 — must use GET for size checks.
- FTP upload success = curl HTTP code **226**.
- Thomson FTP articles dir = `articles/`; Loyang same. Both standalone FTP accounts.

### Status / Next
- All 89 URLs submitted via IndexNow; Google re-crawl takes 1–3 weeks.
- #1 remaining lever: **Google Search Console API access** (none in workspace). Asked Jetlee for GSC access to request indexing per-page. Pending his reply.
- GSC verification for jetleechannel.sg was previously pending (from memory logs).

## Balance Units automation (carried from Aug 4, confirmed running)
- Daily 9am SGT crons live for both `scripts/update-balance-units.mjs` (19 sites) + `scripts/update-granddunman.mjs`. Logs: `balance-units/cron.log`, `granddunman/cron.log`.
- Check first unattended cron run after 9am to confirm all 19 upload OK.
- 6 sites still lack ecoprop data (amberwood, lucernegrand, generations-tannery, TheSierra, thomsonreserve, hougangcentral) — need share links from Jetlee.
# 2026-08-05 Daily Notes

## Grand Dunman facts-grid count bug — FIXED
- Found: script updates hero stat (79) but NOT the facts-grid "Available Units" div (still said 80 Units — was wrong since initial build)
- Fixed scripts/update-granddunman.mjs: added replace for `<div class="fact-label">Available Units</div><div class="fact-value">N Units</div>` pattern
- Fixed live: sed local granddunman/index.html 80→79, re-uploaded via FTP (curl -T), verified live = 79 Units in all 4 spots (hero stat, facts grid, intro, FAQ) + Last Updated 5 Aug 2026
- Cron 9am run confirmed working: balance-units (19 sites) + granddunman both ran 9:00:01, logs created
- Grand Dunman detected real change: 80→79 units (Penthouse 7→6), Telegram notify HTTP 200
