# 2026-08-08

## Amberwood logo link fix — "AtH" went to jetleechannel.sg
- Jetlee: clicking the "AtH" logo at top-left of jetleechannel.sg/amberwood/ went to jetleechannel.sg (wrong).
- Cause: logo `<a href="/">` (root) instead of `/amberwood/`.
- Fixed: logo href → `/amberwood/` in amberwood/index.html (pulled live copy, edited, uploaded to MAIN jetleechannel FTP /amberwood/index.html, code 226).
- Verified live in browser: clicking logo now stays on the Amberwood page. Footer links to HQ are intentional (left as-is).
- Reminder (from Aug 7): amberwood's own FTP subaccount has a stale legacy copy — ALWAYS deploy amberwood to main jetleechannel.sg FTP /amberwood/index.html.

## BelgraviaAce mobile nav — Info dropdown collapsed by default (scroll-up fix)
- Jetlee (screenshot): on mobile, the "Info" dropdown was ALWAYS expanded, pushing FAQ/Contact/New Launch below the fold — "Navigator page Info should be scroll up. Only when we click then it scroll down".
- Cause: mobile media query (max-width:900px) forced `.dropdown { position:static; opacity:1; visibility:visible }` = always open.
- Fix in BelgraviaAce/index.html:
  1. Mobile CSS: `.dropdown` now collapsed by default (max-height:0, opacity:0, visibility:hidden, overflow:hidden) with smooth transition; `.nav-item.dropdown-open .dropdown` expands (max-height:320px).
  2. Caret rotates 180° when open.
  3. JS: click handler on `.nav-item > a` — on mobile (matchMedia ≤900px) preventDefault + toggle `.dropdown-open` (tap to expand; desktop hover unchanged).
- Uploaded to main jetleechannel FTP /BelgraviaAce/index.html (226). Verified live: CSS+JS present, collapsed-by-default + tap-toggle confirmed via DOM test.
- Mobile menu now compact: Home/Overview/Floorplans/Balance Units/Site & Location/Info▼/FAQ/Articles/New Launch/Contact all visible without scrolling.

## BelgraviaAce — floating WhatsApp button added
- Jetlee: "Pls add floatin whatsapp" (after the mobile nav fix).
- Added same .float-wa pattern as Amberwood: fixed bottom-right (32px), 56x56px green (#25D366) circle, 💬 emoji, hover scale 1.1 + glow shadow, z-index 999, links to wa.me/6587649315 with "Hi Jet Lee, I am interested in Belgravia Ace" prefill.
- Placed before <footer>. Uploaded to main jetleechannel FTP /BelgraviaAce/index.html (226). Verified live via DOM: visible, 56x56, bottom 32px right 32px, z-index 999.
